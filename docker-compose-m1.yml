version: "3"

services:
  node:
    build: .
    ports:
      - "80:80"
    volumes:
      - ./:/var/www/html
    command: bash -c "npm install && npm run dev"
  redis:
    image: redis:3.2.11
    ports:
      - "${REDIS_PORT}:6379"
volumes:
  arangodata:
  arangoconfig:
