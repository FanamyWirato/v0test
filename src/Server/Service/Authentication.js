export default class Authentication {
    static salt = 'superD00perC3v4lc4nd0'
    /**
     * @param {string} user
     * @param {string} pw
     */
    login (user, pw) {
        // pepper is the created date of the user as date string with milliseconds

    }

    generatePWHash (pw, pepper) {

    }
}
